// app/(tabs)/plus.tsx
import { Text, View } from 'react-native';

export default function PlusScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#000', justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: '#fff' }}>+ button (placeholder)</Text>
    </View>
  );
}
